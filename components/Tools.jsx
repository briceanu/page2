import React from 'react';
import styles from '../styles/Tools.module.scss';
const Tools = () => {
  return (
    <section id='tools' className={styles.tools_section}>
      <h1 className={styles.hero_text}>tools tools tools tools </h1>
    </section>
  );
};

export default Tools;
